# Nix build outputs
result
result-*

# Temporary extraction directory (created by mrpack-to-nix.sh)
.mrpack-tmp/
*.tmp/

# Extracted .mrpack folders (generated by unzip, not needed in repo)
# Only the .mrpack file itself and datapacks/ should be tracked
Modded\ Survival*/
# When you rename your .mrpack, add the extracted folder name here too

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Secrets — never commit these
*.key
*.pem
*.env
